<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Story Writting App - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-black min-h-screen flex justify-center items-center">
    <!-- Centered Login Box -->
    <div
      class="bg-black border-2 border-gray-700 rounded-2xl shadow-xl p-8 w-full max-w-md"
    >
      <h2
        class="text-3xl text-cyan-400 font-bold text-center md:col-span-2 mb-6 drop-shadow-lg"
      >
        Login
      </h2>

      <form action="/login" method="POST" class="flex flex-col gap-4">
        <input
          type="text"
          name="uniqueId"
          placeholder="Registration Number"
          required
          class="py-2 px-3 rounded-full border-2 border-gray-600 bg-transparent text-orange-500 focus:outline-none"
        />
        <input
          type="password"
          name="password"
          placeholder="Password"
          required
          class="py-2 px-3 rounded-full border-2 border-gray-600 bg-transparent text-orange-500 focus:outline-none"
        />

        <div class="flex items-center gap-5">
          <span
            class="pl-2 flex items-center overflow-hidden border-2 border-gray-600 w-fit rounded-full text-white"
          >
            Captcha:
            <span
              class="relative border-l-2 border-white text-[16px] overflow-hidden bg-gray-200 px-3 py-2 w-fit rounded-full text-white ml-2 opacity-80"
            >
              <%= captcha %>
              <div
                class="absolute top-0 left-0 w-full h-full"
                style="
                  background-color: rgba(0, 0, 0, 0.829);
                  pointer-events: none;
                "
              ></div>
            </span>
          </span>

          <input
            class="py-2 px-3 rounded-full border-2 border-gray-600 bg-transparent text-orange-500 focus:outline-none"
            type="text"
            placeholder="Captcha here..."
            required
            name="captchacode"
          />
        </div>

        <button
          type="submit"
          class="bg-cyan-400 hover:bg-cyan-500 text-black p-2 rounded-full font-semibold"
        >
          Login
        </button>
      </form>

      <p class="mt-3 text-center text-gray-400 text-sm md:col-span-2">
        <a href="/" class="text-cyan-400 hover:underline">Home</a> &nbsp; |
        &nbsp; Don’t have an account?
        <a href="/signup" class="text-cyan-400 hover:underline">Signup</a>
      </p>
    </div>
  </body>
</html>
